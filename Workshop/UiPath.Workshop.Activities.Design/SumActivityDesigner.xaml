<sap:ActivityDesigner x:Class="UiPath.Workshop.Activities.Design.SumActivityDesigner"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:s="clr-namespace:System;assembly=mscorlib"
    xmlns:sap="clr-namespace:System.Activities.Presentation;assembly=System.Activities.Presentation"
    xmlns:sapv="clr-namespace:System.Activities.Presentation.View;assembly=System.Activities.Presentation"
    xmlns:sapc="clr-namespace:System.Activities.Presentation.Converters;assembly=System.Activities.Presentation">
    <sap:ActivityDesigner.Resources>
        <ResourceDictionary>
            <sapc:ArgumentToExpressionConverter x:Key="ArgumentToExpressionConverter" />
        </ResourceDictionary>
    </sap:ActivityDesigner.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0">First number</TextBlock>
        <sapv:ExpressionTextBox Grid.Column="1" Grid.Row="0" OwnerActivity="{Binding Path=ModelItem}" ExpressionType="s:Int32" ToolTip="Cucu bau!" DockPanel.Dock="Top"
            Expression="{Binding Path=ModelItem.FirstNumber,Converter={StaticResource ArgumentToExpressionConverter}, ConverterParameter=In, Mode=TwoWay}"/>

        <TextBlock Grid.Column="0" Grid.Row="1">Second number</TextBlock>
        <sapv:ExpressionTextBox Grid.Column="1" Grid.Row="1" OwnerActivity="{Binding Path=ModelItem}" ExpressionType="s:Int32" ToolTip="Cucu bau!" DockPanel.Dock="Top"
            Expression="{Binding Path=ModelItem.SecondNumber,Converter={StaticResource ArgumentToExpressionConverter}, ConverterParameter=In, Mode=TwoWay}"/>

        <TextBlock Grid.Column="0" Grid.Row="2">Result</TextBlock>
        <sapv:ExpressionTextBox Grid.Column="1" Grid.Row="2" OwnerActivity="{Binding Path=ModelItem}" UseLocationExpression="True" ExpressionType="s:Int32" ToolTip="Cucu bau!" DockPanel.Dock="Top"
            Expression="{Binding Path=ModelItem.SumResult,Converter={StaticResource ArgumentToExpressionConverter}, ConverterParameter=Out, Mode=TwoWay}"/>
    </Grid>
</sap:ActivityDesigner>
